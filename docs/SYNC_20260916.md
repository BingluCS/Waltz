# Production source synchronization — 2026-09-16

This update synchronizes the adopted CDF97/IDWT, WZP and pipeline changes.
It does not adopt the recent quality/entropy-coding experiments and does not
change the default algorithm as part of repository cleanup.

## Scope

- Updated forward/inverse transforms, shared configuration and lossless backend.
- Updated gather/scatter helpers and signed-WZP interfaces/callers.
- Kept the release repository's install/export CMake configuration and its CLI
  help, archive writing and optional reconstruction-file output.
- Normalized public include paths, removed trailing whitespace and guarded
  the existing LC `NDEBUG` definition to avoid a Release-build redefinition warning.
- Updated the optional WZP benchmark for the signed-only encoder API.
- Added an opt-in `WALTZ_BUILD_TESTS` target and build/profiler ignore patterns.
- Excluded local build trees, benchmark snapshots, datasets, backup files,
  research PDFs and standalone experimental P4/dual/quality-codec modules.

## Verification

CUDA 13.1 / H100, `CMAKE_CUDA_ARCHITECTURES=90`, Release:

- Fresh library, CLI, tuner, WZP benchmark and regression-test build: PASS.
- Installation and an external `find_package(Waltz CONFIG REQUIRED)` consumer:
  PASS (compiled, linked and executed).
- WZP regression: 54 cases x 9 calls, PASS.
- Full API comparison with a separate fresh production-source build:
  NYX temperature f32, Miranda density f64, SCALE U f32, Hurricane Uf48 f32,
  and synthetic 65x67x33 float/double with WZP and LC.
  All eight configurations passed three repetitions in each build.
- Final compressed sizes, CR, PSNR, maximum errors and reconstruction hashes
  matched exactly between source and release builds for every configuration.
- Git whitespace check: PASS.

These are correctness checks; they do not establish new cross-GPU performance
claims. The compressed byte sequence itself is not required to be identical
because the parallel encoder can assign payload locations in different orders.

No git commit, push or remote synchronization was performed.
