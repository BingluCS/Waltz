# Production synchronization — 2026-09-17

Source: `/home/lb/compressor/Waltz`.

## Scope

- Synchronized production headers, pipeline, transforms, WZP, CLI, and current
  WZP diagnostic/regression sources. Retained workshop CMake installation and
  package-export support, README, licensing notices and historical sync notes.
- WZP is the only codec backend; removed the obsolete LC backend, unused BIT_4
  component, standalone tuner example and `quant_reorder.hpp`. Inverse offset
  tables now live in `lossless/reorder.hpp`.
- Current compressed format omits dimensions/type; standalone decompression
  requires the caller to supply them. Old development formats are unsupported.
- Includes decode block-offset timing, actual plane-IDWT workspace validation,
  centralized statistics/API e2e reporting and restored CLI reference checks.
- Quantization-outlier repair remains separate. Rejected linear/hash fusion
  experiments were NOT copied. The existing empty unquant event interval is
  unchanged, following the decision to stop that experiment.
- No datasets, benchmark archives, source-tree binaries or experimental modules
  copied. Trailing whitespace normalized in two copied sources only.

## Verification

CUDA 13.1, Release, architecture 90, H100 GPU 1:

- Library, CLI, optional benchmark and both regression targets build: PASS.
- `ctest`: `wzp_roundtrip` and `wzp_fused_decode` both PASS.
- Fresh temporary-prefix installation: PASS.
- Hurricane float (500x500x100), REL 1e-3, two round trips: PASS.
  Compressed size 2,748,768 B, PSNR 72.109090971115,
  reconstruction hash `e5b3ce099a4a7e09`.
- Miranda double (384x384x256), REL 1e-3, two round trips: PASS.
  Compressed size 1,203,576 B, PSNR 78.529200925335,
  reconstruction hash `94f274fc7108105d`.
- Both datasets' final size, PSNR and reconstruction hash match the production
  source results exactly. These checks make no new performance claim.
- Git whitespace check: PASS.

Local verification logs: `/tmp/waltz-workshop-sync-7ijtOt`.
Removed tracked files remain recoverable from this repository's Git history.
No commit, push or remote synchronization performed.
